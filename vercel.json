{
  "installCommand": "npm install",
  "buildCommand": "npm install --prefix backend && npx prisma generate --schema=backend/prisma/schema.prod.prisma && npx prisma db push --schema=backend/prisma/schema.prod.prisma --accept-data-loss && (npx tsx backend/prisma/seed.ts || true) && npm install --prefix frontend && npm run build --prefix frontend",
  "outputDirectory": "frontend/dist",
  "functions": {
    "api/index.ts": {
      "maxDuration": 30,
      "includeFiles": "backend/generated/prisma/**"
    }
  },
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
